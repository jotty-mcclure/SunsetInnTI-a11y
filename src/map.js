import { setAttribute, swapTag } from './_functions';

export default () => {
	if ( window.location.pathname === '/content/sunset-map.aspx' ) {
		document.title = 'A Map of Sunset Inn & Cottages - Sunset Inn & Cottages';
	}
}