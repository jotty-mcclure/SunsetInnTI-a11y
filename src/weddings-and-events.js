import { setAttribute, swapTag } from './_functions';

export default () => {
	if ( window.location.pathname === '/content/weddings-and-events.aspx' ) {
		document.title = 'Weddings and Events - Sunset Inn & Cottages';
	}
}